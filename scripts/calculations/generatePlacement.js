function randomInt(min, max, propObj, roomObj, props) {
  return Math.floor(Math.random() * (max - min + 1) + min)
}
function placeProp(width, height){
let x =  randomInt(50, 1215 - width)
let y = randomInt(65, 570 - height)
function redo(){

}  
}